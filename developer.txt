# Install in development mode
cd turboalias
pip install -e .

# Initialize
turboalias init

# Start using it!
turboalias add ll 'ls -lah'
turboalias add gst 'git status' --category git
turboalias list
```

## ✨ Key Features Implemented

✅ **Commands**:
- `turboalias init` - Setup with auto shell detection
- `turboalias add` - Add aliases with optional categories
- `turboalias remove` - Remove aliases
- `turboalias list` - List all/filtered aliases
- `turboalias categories` - Show all categories
- `turboalias import` - Import existing shell aliases
- `turboalias clear` - Clear all aliases
- `turboalias edit` - Open config in $EDITOR

✅ **Features**:
- JSON config storage in `~/.config/turboalias/`
- Category organization
- Auto-detects bash/zsh
- Generates clean `aliases.sh` file
- Imports existing aliases safely
- Beautiful organized output

## 📁 Project Structure
```
turboalias/
├── turboalias/
│   ├── __init__.py
│   ├── cli.py
│   ├── config.py
│   └── shell.py
├── setup.py
├── pyproject.toml
└── README.md